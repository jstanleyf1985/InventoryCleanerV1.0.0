$(document).ready(function () {
  /* $('#dirtyFileBTN').on('click', function () {
      const modalPath = path.join('file://', __dirname, 'public/html/info.html');
      let win = new BrowserWindow({
        frame: false,
        alwaysOnTop: true,
        width: 400,
        height: 200,
        webPreferences: {
          nodeIntegration: true
        }
      });
      win.on('close', function () {
        win = null
      });
      win.loadURL(modalPath);
      win.show();
    }); */
});